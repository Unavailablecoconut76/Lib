Working features:

`***Admin addition***

`***Setting Profile photos***

***`OTP Validation using JWT tokens(reset password working).***

***`One book at a time policy(check kr)***

***`Registering new user***

***`issuing book(admin enters borrowers email...user checks for if he has book currently...if retuned within time...then no charges else fine/day is 1/-) borrow date issue :displayed as NAN***

***`book addition and deletion***

`Filter by something(how should i even filter without having tags?)





---add email for whom ur sending otp to or whom ur resetting or forgetting password for.

***`now admin can also borrow a book***

---`Add currently borrowed column on registered users section when logged in as admin

---`Fix: Unavailable if book quantity is set to 0.

---`Block user

`send mail reminder to return book in due time

`Settings menu for superuser access to Database, admin removal, reset books, reset user history(auto for a month?) and general like Dark mode(?)

`import csv file for book upload section

`Maybe integrate AI to look for searching(books)?(Maths->math data structures->dsa,speak to win->english/speaking)

`Fetch only 25 books at a time then if clicked on page 2 then fetch more... but dont fetch the whole database for efficient resource management

